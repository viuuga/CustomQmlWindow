pragma Singleton

import QtQuick

QtObject {
    property string closeWindowAction: "closeWindowAction"
    property string maximizeWindowAction: "maximizeWindowAction"
    property string minimizeWindowAction: "minimizeWindowAction"
}
